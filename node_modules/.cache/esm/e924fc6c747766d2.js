let minimist,chalk,help,add,del,search,autocomplete,print_trie;_747‍.x([["cli",()=>cli]]);_747‍.w("minimist",[["default",["minimist"],function(v){minimist=v}]]);_747‍.w("chalk",[["default",["chalk"],function(v){chalk=v}]]);_747‍.w("./help",[["help",["help"],function(v){help=v}]]);_747‍.w("./add",[["add",["add"],function(v){add=v}]]);_747‍.w("./del",[["del",["del"],function(v){del=v}]]);_747‍.w("./search",[["search",["search"],function(v){search=v}]]);_747‍.w("./autocomplete",[["autocomplete",["autocomplete"],function(v){autocomplete=v}]]);_747‍.w("./print_trie",[["print_trie",["print_trie"],function(v){print_trie=v}]]);


 





const validate = (argsObj, cmd) => {
    if (cmd == 'print') return false;
    if (!('keyword' in argsObj)) {
        console.error(`\nMissing keyword. Please use \n"trie ${cmd} --keyword=${chalk.blueBright('keyword')}"\n`);
        return true;
    }
    return false;
}

       async function cli(args) {
    const argsObj = minimist(args.slice(2));
    let cmd = argsObj._[0] || 'help'; 
    // console.log(argsObj['keyword']);
    let res = validate(argsObj, cmd); 
    if (res == true) return; 
    switch (cmd) {

        case 'add': 
            add(argsObj);
            break;

        case 'delete': 
            del(argsObj);
            break;

        case 'help': 
            help(argsObj); 
            break;

        case 'search': 
            search(argsObj);
            break;
        
        case 'autocomplete': 
            autocomplete(argsObj);
            break;

        case 'print': 
            print_trie(argsObj); 
            break;
        
        default: 
            console.error(`"${cmd}" is not a command! Use ${chalk.blueBright('trie help')} to see a list of commands`)
    }
}